import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // Firebase requires real credentials; integration tests live in
    // integration_test/ and are run against a real device or emulator.
    expect(true, isTrue);
  });
}
